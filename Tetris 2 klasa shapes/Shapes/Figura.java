package Shapes;

import java.awt.*;

public class Figura {
    Color[] colors = [COLOR.RED, COLOR.BLUE, ...];
    Shape shape = Shape.createShape()
    Shape nextShape = Shape.createShape()
    int [][]tab = new int[20][15] // same 0 na poczatku, inna liczba ma swoj odpowiedni kolor

    int movedY; // o ile pol spadl w dol, zwiekszasz o 1

    public void nextTick() {
        // if mozna isc w dol {
        //   movedY++
        // } else {
        // dopisac shape do tab
        //  shape = nextShape
        //  nextShape = Shape.createShape()
        // }
    }
}